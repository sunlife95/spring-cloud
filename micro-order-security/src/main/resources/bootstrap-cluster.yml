spring:
  application:
    name: micro-order

eureka:
  client:
    service-url:
      defaultZone: http://admin:admin@localhost:8761/eureka/,http://admin:admin@localhost:8762/eureka/
    healthcheck:
      enabled: true
server:
  port: 8081

